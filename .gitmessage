Fix: Magic Merge now returns full MergeSession, not just result counts

Rust magic_merge command now returns MergeSession instead of
MagicMergeResult, so the frontend can use the resolved state
directly without re-reading from disk. Frontend tauri.ts and
App.tsx updated accordingly.
