# Splice — Test Workflow
# Runs quality checks on push to master/dev and on all pull requests.
name: Test

on:
  push:
    branches: [master, dev]
  pull_request:
    branches: ["*"]

jobs:
  quality:
    uses: ./.github/workflows/test-and-lint.yml
